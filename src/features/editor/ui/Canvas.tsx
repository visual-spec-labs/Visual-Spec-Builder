/** 중앙 캔버스 — GUI 결과 확인·편집 영역. 지금은 자리만 잡는다. */
export function Canvas() {
  return (
    <main className="flex items-center justify-center overflow-auto bg-surface-canvas [grid-area:canvas]">
      <p className="text-content-subtle">캔버스</p>
    </main>
  );
}
